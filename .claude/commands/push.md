First, follow all the steps in `.claude/commands/commit.md`. If there are no changes to commit, skip directly to the next step.

Once the commit step is done (or skipped), run `git push` and display the result.
